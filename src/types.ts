export interface Movie {
  Title: string;
  Year: string;
  Country: string;
}
export interface CountryInfo {
  flagUrl: string;
  currency: string;
}
export interface FilmInfo {
  duration: number;
  country: string;
}
